# Dev: ไม่ต้องตั้งก็ได้ — fallback เป็น local libSQL file (file:./local.db)
# Prod (Turso): ตั้ง 2 ตัวนี้ให้ครบ
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# ชื่อ cookie ของ session (default: es_session)
SESSION_COOKIE_NAME=es_session
